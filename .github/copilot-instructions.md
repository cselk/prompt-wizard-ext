For all UI work, refer to `DESIGN_GUIDE.md` in the repo root for the complete
visual design system — tokens, typography, spacing, component rules, and what to avoid.
Never hardcode color values; always use the CSS custom properties defined there.
